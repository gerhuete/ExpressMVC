<div class="container">
    <div class="card text-center">
        <div class="card-header">
            {{title}}
        </div>
        <div class="card-body">
            <p class="card-text">{{message}}</p>
            <a href={{redirectTo}} class="btn btn-primary">Home</a>
        </div>
        </div>
</div>